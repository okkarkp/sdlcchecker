# Implementation — <ticket>

> Authors: backend-developer and frontend-developer (each appends; do not overwrite the other).

## Backend
What was built, key classes/endpoints, schema migration(s) added with the entity, non-obvious
choices a reviewer needs to know.

## Frontend
Components/hooks/data-layer added, access-control gating, BFF/middleware changes, non-obvious choices.
