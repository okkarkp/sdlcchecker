# UI flow — <ticket>

> Author: frontend-designer (returned output, persisted by orchestrator). Consumed by frontend-developer.

## Screen flow
Step-by-step navigation, entry/exit points, and states (empty / loading / error / success).

## Component spec
Per screen: components, props, layout, which existing components are reused vs new.

## Design tokens
Colors / spacing / typography to use; design-system references.
