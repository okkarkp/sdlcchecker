# Tests — <ticket>

> Author: test-engineer.

## Test plan
What is covered at each layer and why (per-AC, derived from the spec — not just current behaviour).

## Results
- **Unit:** <command> — <pass/fail, coverage>
- **Integration:** <results>
- **E2E (browser/API):** RAN | SKIPPED — reason (suite not initialised / stack not up / no seed data).

> Follow the code over the testing-guide: if a doc names a test target that does not exist,
> skip it and flag the drift — do not force it.
